"use client";

// FlowSimBar is intentionally empty — the Flow canvas shares
// the same SimulationBar as the Excalidraw canvas (rendered by CanvasPage).
// This file exists as a placeholder so FlowCanvas.tsx can import it
// without error if needed later.

export default function FlowSimBar() {
  return null;
}
